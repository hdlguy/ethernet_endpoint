# ethernet_endpoint
A project to implement an Ethernet endpoint in FPGA fabric. ARP and UDP will be implemented.

## Using
Clone the repo and update the Alex Forencich submodule.
```
git clone https://github.com/hdlguy/ethernet_endpoint.git
cd ethernet_endpoint
git submodule update --init --recursive
```

